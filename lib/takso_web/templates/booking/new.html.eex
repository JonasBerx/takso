<h2>New booking</h2>

<%= render "form.html", changeset: @changeset,
                        action: Routes.booking_path(@conn, :create_booking) %>

<span><%= link "Back", to: Routes.booking_path(@conn, :display_booking_list) %></span>
